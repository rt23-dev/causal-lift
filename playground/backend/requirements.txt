# Playground depends on the library installed in editable mode from the repo root:
#   pip install -e ..
# Or via PyPI once published:
#   causal-lift>=0.1.0

fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic==2.7.1

# Library deps (also pulled transitively by causal-lift):
pandas>=2.0
numpy>=1.24
statsmodels>=0.14
